import type { Settings } from "@/types/Settings";

export const defaultSettings: Settings = {
  companyName: "Aastha Engineering",
  notifications: true,
  darkMode: false,
  autoBackup: true,
  backupFrequency: "Daily",
  defaultMachine: "Machine 1",
};
